# Copy to .env and change POSTA_SECRET before deploying.
# Generate: python -c "import secrets; print(secrets.token_urlsafe(48))"

POSTA_SECRET=change-me-to-a-long-random-string
POSTA_DB_PATH=./data/posta.db
POSTA_HOST=0.0.0.0
POSTA_PORT=8000
